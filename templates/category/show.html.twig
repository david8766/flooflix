{% extends 'base.html.twig' %} {% block title %}Données de la catégorie{% endblock %} {% block body %}
<div class="container">
    <h1>Données de la catégorie:</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ category.genre }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn btn-primary btn-lg" href="{{ path('category_edit', {'id': category.id}) }}">Modifier</a>
    <p>ou</p>
    {{ include('category/_delete_form.html.twig') }}
    <hr>
    <a class="btn btn-primary btn-lg" href="{{ path('category_index') }}">Retour à la liste</a>
    <a class="btn btn-primary btn-lg" href="/">Retour à l'accueil du site</a>
</div>
{% endblock %}