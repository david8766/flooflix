{% extends 'base.html.twig' %} {% block title %}Modifier{% endblock %} {% block body %}
<div class="container">
    <h1>Modifier un utilisateur</h1>

    <br> {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
    <p>ou</p>
    {{ include('user/_delete_form.html.twig') }}
    <hr>
    <a class="btn btn-primary btn-lg" href="{{ path('user_index') }}">Retour à la liste</a>
    <a class="btn btn-primary btn-lg" href="/">Retour à l'accueil du site</a>
</div>
{% endblock %}
