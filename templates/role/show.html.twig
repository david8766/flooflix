{% extends 'base.html.twig' %} {% block title %}Données du role{% endblock %} {% block body %}
<div class="container">
    <h1>Role</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ role.role }}</td>
            </tr>
        </tbody>
    </table>


    <a class="btn btn-primary btn-lg" href="{{ path('role_edit', {'id': role.id}) }}">Modifier</a>
    <p style="padding-top:1em">ou</p>
    {{ include('role/_delete_form.html.twig') }}
    <hr>
    <a class="btn btn-primary btn-lg" href="{{ path('role_index') }}">Retour à la liste des roles</a>
    <a class="btn btn-primary btn-lg" href="/">Retour à l'accueil du site</a>
</div>
{% endblock %}